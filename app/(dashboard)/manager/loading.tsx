import { SkeletonHeader, SkeletonList } from '@/components/skeleton'

export default function Loading() {
  return (
    <section>
      <SkeletonHeader />
      <SkeletonList />
    </section>
  )
}
