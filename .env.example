# Public, safe for client bundle
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Secret, server-only. Never referenced outside services/supabase/admin.ts
SUPABASE_SERVICE_ROLE_KEY=

# Secret, migration jobs only (GitHub Actions environment secrets in CI)
SUPABASE_DB_URL=

# Secret, used only inside Supabase Edge Functions (Phase 6), never here
# AI_PROVIDER_API_KEY=
