# Page overrides

One file per page, named for its route segment — `dashboard.md`, `pricing.md`.
Rules here take precedence over `../MASTER.md` for that page only.

Override sparingly. A page that needs to break the master system is usually a page
that has been designed wrong. Record the reason in the file.
